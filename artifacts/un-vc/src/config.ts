// Base URL of the UN-VC backend API (hosted on Render).
// The frontend and backend are deployed separately, so every API call must
// target this absolute URL instead of the site's own origin.
export const API_BASE = "https://un-vc-api.onrender.com";
